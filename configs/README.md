# 配置目录 (Configs)

本目录存储项目级别的全局配置文件。

## 配置文件

- `global_config.yaml` - 全局项目配置
- `paths.yaml` - 数据路径配置
- `training_config.yaml` - 通用训练配置
- `experiment_config.yaml` - 实验配置

## 使用说明

各模块也有自己的configs/子目录，用于存储模块特定的配置。
本目录用于存储跨模块共享的配置参数。

建议使用YAML或JSON格式，便于阅读和维护。
